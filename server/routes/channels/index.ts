import type { Express } from "express";

export function registerChannelRoutes(app: Express) {
  // Channel management routes will be implemented here
  // Currently handled by existing channel system
}