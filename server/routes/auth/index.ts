import type { Express } from "express";

export function registerAuthRoutes(app: Express) {
  // Auth routes are handled by setupAuth() in auth.ts
  // This module can be extended for additional auth-related endpoints
}